PYZZLE_INSTRUCTIONS_START
Skriv ett program som skriver ut 
alla primtal från 2 till 100
PYZZLE_INSTRUCTIONS_END
PYZZLE_CODE_START
nr = 2
while nr <= 100:
    nr_prime = True
    divisor = nr - 1
    divisor = nr
    while divisor > 1:
        if nr % divisor == 0:
            nr_prime = False
            break
        divisor = divisor - 1
    nr += 1
    if not nr_prime:
    if nr_prime == True:
        print(nr, end=", ")
    nr = nr + 1

PYZZLE_CODE_END